@model IEnumerable<WebBanHangOnline.Models.EF.SocialMediaPlugin>

@{
    ViewBag.Title = "Partial_FooterSocialMediaPlugin";
}

@if (Model != null && Model.Any())
{
    foreach (var item in Model)
    {
        @Html.Raw(@item.Plugin)
    }
}
