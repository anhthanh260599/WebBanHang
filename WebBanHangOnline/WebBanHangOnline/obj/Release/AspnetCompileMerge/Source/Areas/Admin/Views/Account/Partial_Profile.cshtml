@model WebBanHangOnline.Models.CreateAccountrViewModel

@if (Model != null)
{
    <a href="@Url.Action("Index","Home")">
        <div class="image">
            <img src="@Model.Avatar" class="img-circle elevation-2" alt="User Image">
            <span class="ml-2 text-md-center">@Model.FullName</span>
        </div>
    </a>
}
